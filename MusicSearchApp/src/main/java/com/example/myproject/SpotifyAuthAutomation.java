package com.example.myproject;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

public class SpotifyAuthAutomation {
	/*
	public String getAuthorizationCode(String authUri) {
        // WebDriverのパスを指定
        System.setProperty("webdriver.chrome.driver", "path/to/chromedriver");

        // Chromeを起動
        WebDriver driver = new ChromeDriver();

        // Spotifyの認証URIを開く
        driver.get(authUri);

        // ユーザーが手動で認証コードを入力する時間を与える
        try {
            Thread.sleep(5000); // 5秒待機（必要に応じて調整）
        } catch (InterruptedException e) {
            e.printStackTrace();
        }

        // 認証コードを取得
        WebElement codeElement = driver.findElement(By.name("code"));
        String code = codeElement.getAttribute("value");

        // ブラウザを閉じる
        driver.quit();

        return code;
    }*/	
}